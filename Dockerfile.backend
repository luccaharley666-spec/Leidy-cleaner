# Dockerfile.backend - Simples e funcional
FROM node:24-alpine

WORKDIR /app

# Copy everything
COPY . .

# Install dependencies
RUN npm install --legacy-peer-deps --production 2>&1 | grep -v "deprecated" || true

# Expose port
EXPOSE 3000

# Start server
CMD ["node", "src/index.js"]
