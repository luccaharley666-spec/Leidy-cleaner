# ========================================
# 🚀 PRODUCTION ENVIRONMENT CONFIGURATION
# ========================================
# Generated: February 10, 2026
# Version: PROD SECURE v3

# ========================================
# 🔑 AUTHENTICATION & SECURITY
# ========================================

# JWT Secret (GERAR NOVO COM: openssl rand -hex 32)
JWT_SECRET=<GENERATE_WITH:openssl rand -hex 32>

# JWT Expiration
JWT_EXPIRATION=24h
[REDACTED_TOKEN]=7d

# Session Secret (Gerar novo: openssl rand -hex 32)
SESSION_SECRET=$(openssl rand -hex 32)

# ========================================
# 🌐 ENVIRONMENT & SERVER
# ========================================

# Node Environment
NODE_ENV=production

# Backend
PORT=3001
HOST=0.0.0.0
API_BASE_URL=https://seu-dominio.com/api

# Frontend
NEXT_PUBLIC_API_URL=https://seu-dominio.com/api
NEXT_PUBLIC_WS_URL=wss://seu-dominio.com

# ========================================
# 💾 DATABASE CONFIGURATION
# ========================================

# PostgreSQL (RECOMENDADO PARA PRODUÇÃO)
DATABASE_URL=postgresql://usuario:senha@postgres-host:5432/seu_banco

# SQLite (DESENVOLVIMENTO APENAS)
# DATABASE_URL=sqlite://./backend_data/database.sqlite

# Database Pool
DB_POOL_MIN=2
DB_POOL_MAX=10

# ========================================
# 🔴 REDIS CONFIGURATION
# ========================================

# Redis (OBRIGATÓRIO PARA FILAS PERSISTENTES)
REDIS_URL=redis://redis-host:6379

# Redis Options
REDIS_KEY_PREFIX=leidy:
REDIS_DB=0

# ========================================
# 💳 PAGAMENTOS
# ========================================

# Stripe Production Keys
[REDACTED_TOKEN]=<PLACEHOLDER:[REDACTED_TOKEN]>
[REDACTED_TOKEN]=<PLACEHOLDER:[REDACTED_TOKEN]>

# Webhook Secret
[REDACTED_TOKEN]=<PLACEHOLDER:[REDACTED_TOKEN]>

# ========================================
# 🏦 PIX INTEGRATION
# ========================================

# PIX Webhook
PIX_WEBHOOK_URL=https://seu-dominio.com/api/webhooks/pix
[REDACTED_TOKEN]=<GENERATE_WITH:openssl rand -hex 32>

# PIX Keys (do provider - Ex: Asaas, Pagbank, etc)
PIX_PROVIDER_KEY=<PLACEHOLDER:PIX_PROVIDER_KEY>
PIX_PROVIDER_SECRET=<PLACEHOLDER:PIX_PROVIDER_SECRET>

# ========================================
# 📧 EMAIL CONFIGURATION
# ========================================

# SMTP Configuration
SMTP_HOST=<PLACEHOLDER:smtp_host>
SMTP_PORT=587
SMTP_USER=<PLACEHOLDER:smtp_user>
SMTP_PASS=<PLACEHOLDER:smtp_pass>

# Email From
EMAIL_FROM=noreply@seu-dominio.com
EMAIL_SUPPORT=suporte@seu-dominio.com

# ========================================
# 🔔 SENTRY ERROR TRACKING
# ========================================

# Sentry DSN (obter em https://sentry.io)
SENTRY_DSN=<PLACEHOLDER:SENTRY_DSN>
SENTRY_ENVIRONMENT=production
SENTRY_RELEASE=1.0.0
[REDACTED_TOKEN]=0.1

# ========================================
# 📊 MONITORAMENTO & OBSERVABILIDADE
# ========================================

# Logging
LOG_LEVEL=info
LOG_FORMAT=json

# Prometheus Metrics
PROMETHEUS_ENABLED=true
PROMETHEUS_PORT=9090

# Health Check
[REDACTED_TOKEN]=30000

# ========================================
# 🔐 CORS & SECURITY
# ========================================

# CORS Origins (separadas por vírgula)
CORS_ORIGINS=https://seu-dominio.com,https://www.seu-dominio.com

# Rate Limiting
[REDACTED_TOKEN]=900000
[REDACTED_TOKEN]=100

# ========================================
# 📱 PUSH NOTIFICATIONS (Opcional)
# ========================================

# Firebase Cloud Messaging
FIREBASE_PROJECT_ID=seu-projeto
[REDACTED_TOKEN]=sua-chave-privada
[REDACTED_TOKEN]=firebase@seu-projeto.iam.gserviceaccount.com

# ========================================
# 🔂 BACKUP & STORAGE
# ========================================

# AWS S3 (para backups)
AWS_REGION=sa-east-1
[REDACTED_TOKEN]=<PLACEHOLDER:[REDACTED_TOKEN]>
[REDACTED_TOKEN]=<PLACEHOLDER:[REDACTED_TOKEN]>
AWS_S3_BUCKET=<PLACEHOLDER:AWS_S3_BUCKET>

# Backup Schedule (cron format)
BACKUP_SCHEDULE=0 2 * * *

# Backup Retention (dias)
[REDACTED_TOKEN]=30

# ========================================
# 🔗 INTEGRATIONS
# ========================================

# Google APIs (opcional)
GOOGLE_CLIENT_ID=<PLACEHOLDER:GOOGLE_CLIENT_ID>
[REDACTED_TOKEN]=<PLACEHOLDER:[REDACTED_TOKEN]>

# Twilio (SMS opcional)
TWILIO_ACCOUNT_SID=<PLACEHOLDER:TWILIO_ACCOUNT_SID>
TWILIO_AUTH_TOKEN=<PLACEHOLDER:TWILIO_AUTH_TOKEN>
TWILIO_PHONE_NUMBER=<PLACEHOLDER:TWILIO_PHONE_NUMBER>

# ========================================
# 🎯 FEATURE FLAGS
# ========================================

# Features
FEATURE_2FA=true
FEATURE_CHAT=true
FEATURE_PAYMENTS=true
FEATURE_PIX=true
FEATURE_STRIPE=true

# ========================================
# 🔐 SSL/TLS CONFIGURATION
# ========================================

# SSL Certificates (Let's Encrypt / Certbot)
SSL_CERT_PATH=/etc/letsencrypt/live/seu-dominio.com/fullchain.pem
SSL_KEY_PATH=/etc/letsencrypt/live/seu-dominio.com/privkey.pem

# HTTPS
HTTPS_ENABLED=true
HTTPS_PORT=443

# HSTS (HTTP Strict Transport Security)
HSTS_MAX_AGE=31536000
[REDACTED_TOKEN]=true

# ========================================
# 📝 NOTAS IMPORTANTES
# ========================================

# 1. SEGURANÇA:
#    - Nunca commitar este arquivo em Git
#    - Usar diferentes valores para dev/staging/prod
#    - Rotacionar JWT_SECRET regularmente
#    - Usar secrets manager (AWS Secrets Manager, Azure Key Vault)

# 2. DATABASE:
#    - Usar PostgreSQL em produção (não SQLite)
#    - Configurar backups automáticos
#    - Usar SSL/TLS para conexão BD
#    - Aplicar encryption at rest

# 3. REDIS:
#    - Em produção, usar Redis cluster ou AWS ElastiCache
#    - Habilitar persistência (RDB/AOF)
#    - Configurar maxmemory policy

# 4. SENTRY:
#    - Setup alertas para erros críticos
#    - Configurar jq/replay de sessões
#    - Integrar com Slack/PagerDuty

# 5. BACKUPS:
#    - Testar restauração regularmente
#    - Usar múltiplas regiões/buckets
#    - Documentar RTO (Recovery Time Objective)

# 6. SSL/HTTPS:
#    - Renovar certificados 30 dias antes da expiração
#    - Usar A+ rating em SSL Labs
#    - Habilitar HSTS
#    - Implementar certificate pinning (opcional)

# 7. MONITORAMENTO:
#    - Setup alertas para uptime
#    - Monitorar latência de BD
#    - Verificar saúde de filas
#    - Dashboard Prometheus + Grafana
