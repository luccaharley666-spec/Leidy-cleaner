# 🚀 PRODUCTION ENVIRONMENT CONFIGURATION
# Copy this to .env.production and update values for your deployment

# === CRITICAL: Backend API Configuration ===
# Set this to your production backend API URL (Railway, AWS, GCP, etc.)
# Example: https://api.example.com or https://api-leidycleaner.railway.app
NEXT_PUBLIC_API_URL=https://[REDACTED_TOKEN].com

# API timeout in milliseconds (increase for production stability)
[REDACTED_TOKEN]=30000

# === Environment Mode ===
# NEVER use 'development' in production!
[REDACTED_TOKEN]=production
NEXT_PUBLIC_DEBUG=false
[REDACTED_TOKEN]=error

# === Stripe Payment Integration ===
# Get these from your Stripe Dashboard (https://dashboard.stripe.com)
[REDACTED_TOKEN]=[REDACTED_TOKEN]

# === Sentry Error Tracking (Optional but recommended) ===
# For production error monitoring
[REDACTED_TOKEN]=https://your-sentry-dsn@o123456.ingest.sentry.io/123456

# === Feature Flags ===
# These control which features are active
[REDACTED_TOKEN]=true
[REDACTED_TOKEN]=true
[REDACTED_TOKEN]=true

# === Analytics (Optional) ===
NEXT_PUBLIC_GA_ID=G_your_analytics_id

# === Site Information ===
[REDACTED_TOKEN]=https://your-site-domain.com
[REDACTED_TOKEN]=Leidy Cleaner

# ⚠️  IMPORTANT SECURITY REMINDERS:
# 1. NEVER commit .env.production to git (already in .gitignore)
# 2. Set these variables in your hosting platform:
#    - Vercel: Settings → Environment Variables
#    - Railway: Variables tab
#    - Netlify: Build & Deploy → Environment
# 3. Use different API URLs for staging vs production
# 4. Rotate Stripe keys periodically
# 5. Monitor error logs in Sentry dashboard
